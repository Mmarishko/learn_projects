import { AddFavePipePipe } from './add-fave-pipe.pipe';

describe('AddFavePipePipe', () => {
  it('create an instance', () => {
    const pipe = new AddFavePipePipe();
    expect(pipe).toBeTruthy();
  });
});
